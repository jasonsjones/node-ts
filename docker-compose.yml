version: '2'

services:
    node:
        container_name: server
        build:
            context: .
        volumes:
            - .:/home/nodets
            - /home/nodets/node_modules
        ports:
            - "3000:3000"
        depends_on:
            - mongo

    mongo:
        container_name: database
        image: mongo
        volumes:
            - ./data/db:/data/db
        ports:
            - "27017:27017"

networks:
    local-network:
        driver: bridge
